# 日志系统文档

## 概述

本项目已集成完整的日志系统，使用Python的`logging`模块实现。所有控制台输出（`print`语句）已被替换为日志记录调用，确保运行过程中的所有信息都被记录到日志文件中。

## 主要特性

### 1. 自动时间戳文件命名
- 日志文件存储在`logs/`目录下
- 文件名格式：`YYYYMMDD_HHMMSS_mmm.log`（精确到毫秒）
- 例如：`20251231_093001_219.log`
- 支持高并发场景（如benchmark.py同时启动100个进程）

### 2. 双重输出
- **控制台输出**：可选择是否输出到控制台
- **文件记录**：始终记录到日志文件
- 通过`print_to_console`参数控制

### 3. 多进程安全
- 每个进程/线程独立的日志文件
- 使用线程本地存储（Thread-local storage）
- 避免多进程写入冲突

### 4. 多级别日志
支持标准日志级别：
- `DEBUG`: 调试信息
- `INFO`: 一般信息（默认）
- `WARNING`: 警告信息
- `ERROR`: 错误信息
- `CRITICAL`: 严重错误

## 使用方法

### 基本用法

```python
from utils.logger import log_print

# 记录到日志文件并输出到控制台
log_print("这是一条日志消息", print_to_console=True)

# 仅记录到日志文件，不输出到控制台
log_print("仅记录不显示", print_to_console=False)

# 多参数
log_print("参数1:", value1, "参数2:", value2, print_to_console=True)
```

### 指定日志级别

```python
from utils.logger import log_print

log_print("普通信息", print_to_console=True, level='INFO')
log_print("警告信息", print_to_console=True, level='WARNING')
log_print("错误信息", print_to_console=True, level='ERROR')
```

### 便捷函数

```python
from utils.logger import info, warning, error, debug, critical

info("这是INFO级别日志", print_to_console=True)
warning("这是WARNING级别日志", print_to_console=True)
error("这是ERROR级别日志", print_to_console=True)
```

### 获取日志文件路径

```python
from utils.logger import get_log_filename

log_file = get_log_filename()
print(f"当前日志文件: {log_file}")
```

## 日志格式

### 文件格式（增强版）
```
================================================================================
                              AlphaSolve 日志系统
================================================================================
启动时间: 2025-12-31 09:30:01.220
日志文件: 20251231_093001_219.log
================================================================================

2025-12-31 09:30:01.220 │ INFO     │ 📝 [solver] solver generated new conjecture ...
2025-12-31 09:30:02.150 │ METRIC   │ 📊 using: 1.2s, answer length: 1523, cot length: 2341
2025-12-31 09:30:02.151 │ WARNING  │ ⚠️ [verifier] verification failed
2025-12-31 09:30:02.152 │ ERROR    │ ❌ [refiner] refine quota exhausted
2025-12-31 09:30:02.153 │ SUCCESS  │ ✅ [verifier] conjecture verified successfully
```

**格式说明：**
- **文件头部**：包含启动时间、日志文件名等元信息
- **时间戳**：精确到毫秒，格式为 `YYYY-MM-DD HH:MM:SS.mmm`
- **日志级别**：INFO/WARNING/ERROR/SUCCESS/METRIC等（8字符宽度对齐）
- **可视化图标**：每条日志带有对应的emoji图标，提高可读性
  - 🔍 DEBUG - 调试信息
  - 📝 INFO - 一般信息
  - ⚠️ WARNING - 警告信息
  - ❌ ERROR - 错误信息
  - 🔥 CRITICAL - 严重错误
  - ✅ SUCCESS - 成功操作
  - 📊 METRIC - 指标数据
- **模块标识**：自动识别和美化模块标签（如[solver]、[verifier]）
- **消息内容**：实际的日志信息

### 控制台格式
控制台输出格式更简洁，直接显示消息内容，不包含时间戳和日志级别前缀。

## 新增功能

### 1. 分隔线
用于分隔不同阶段或模块的日志：

```python
from utils.logger import log_separator

log_separator('line', width=100, print_to_console=True)      # ──────────...
log_separator('double', width=100, print_to_console=True)    # ══════════...
log_separator('section', width=100, print_to_console=True)   # ━━━━━━━━━━...
log_separator('dash', width=100, print_to_console=True)      # ┈┈┈┈┈┈┈┈┈┈...
log_separator('dot', width=100, print_to_console=True)       # ··········...
```

### 2. 分节标题
输出带标题的分节，用于标识主要阶段：

```python
from utils.logger import log_section

log_section("Solver Phase", width=100, print_to_console=True)
# 输出：
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# │                              Solver Phase                                   │
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3. 边框消息
输出带边框的重要消息：

```python
from utils.logger import log_box

log_box("Task completed successfully!", width=60, print_to_console=True)
# 输出：
# ┌──────────────────────────────────────────────────────────┐
# │ Task completed successfully!                             │
# └──────────────────────────────────────────────────────────┘
```

### 4. 指标记录
专门用于记录性能指标、统计数据等：

```python
from utils.logger import log_metric

log_metric("执行时间", 1.23, "s", print_to_console=True)
log_metric("答案长度", 1523, print_to_console=True)
# 输出：
# 📊 执行时间: 1.23s
# 📊 答案长度: 1523
```

### 5. 字典美化输出
结构化输出字典数据：

```python
from utils.logger import log_dict

config = {
    "model": "gpt-4",
    "temperature": 0.7,
    "max_tokens": 2000
}
log_dict(config, title="配置信息", print_to_console=True)
# 输出：
# ┌─ 配置信息
# │ model: gpt-4
# │ temperature: 0.7
# │ max_tokens: 2000
# └─
```

### 6. 模块标识参数
可以显式指定模块名称，使日志更清晰：

```python
from utils.logger import log_print

log_print("开始求解...", module="solver", print_to_console=True, level='INFO')
# 输出：
# 📝 [solver] 开始求解...
```

## 项目集成情况

已在以下文件中替换所有`print`语句：

### 核心模块
- `main.py` - 主程序入口
- `workflow.py` - AlphaSolve工作流
- `benchmark.py` - 基准测试（支持多进程）

### Agent模块
- `agents/solver.py` - 求解器Agent
- `agents/verifier.py` - 验证器Agent
- `agents/refiner.py` - 改进器Agent
- `agents/summarizer.py` - 总结器Agent
- `agents/utils.py` - Agent工具函数
- `agents/common_agent_base.py` - Agent基类

### LLM模块
- `llms/utils.py` - LLM客户端工具
- `llms/tools.py` - 工具函数（保留内部print用于代码执行）

## 多进程场景

在`benchmark.py`中使用多进程时：
- 每个worker进程创建独立的日志文件
- 日志文件名包含毫秒级时间戳，确保唯一性
- 主进程和worker进程的日志分别记录

示例：运行`benchmark.py -n 100`时，会生成约100个独立的日志文件。

## 注意事项

1. **日志目录**：首次运行会自动创建`logs/`目录
2. **文件清理**：日志文件不会自动清理，需要手动管理
3. **编码**：日志文件使用UTF-8编码
4. **性能**：日志记录是异步的，不会显著影响程序性能
5. **print_to_console参数**：
   - `True`: 同时输出到控制台和文件
   - `False`: 仅记录到文件

## 测试

运行测试脚本验证日志系统：

```bash
python test_logging.py
```

测试内容包括：
- 基本日志输出
- 控制台/文件输出控制
- 多参数支持
- 不同日志级别
- 日志文件验证

## Git配置

`.gitignore`已配置忽略`logs/`目录，避免提交大量日志文件到版本控制系统。

## 使用实例

### 实例1：记录Agent执行过程
```python
from utils.logger import log_section, log_print, log_metric, success

# 开始新阶段
log_section("Solver Agent Execution", print_to_console=True)

# 记录过程
log_print('[solver] 开始生成conjecture...', print_to_console=True)
log_print('[solver] 调用LLM...', print_to_console=True, level='INFO')

# 记录指标
log_metric("执行时间", 2.5, "s", print_to_console=True)
log_metric("答案长度", 1523, print_to_console=True)

# 记录成功
success('[solver] conjecture生成成功！', print_to_console=True)
```

### 实例2：记录配置信息
```python
from utils.logger import log_dict, log_separator

log_separator('double', width=100, print_to_console=True)
config = {
    "solver_model": "gpt-4",
    "verifier_model": "gpt-4",
    "max_rounds": 5
}
log_dict(config, title="AlphaSolve配置", print_to_console=True)
log_separator('double', width=100, print_to_console=True)
```

### 实例3：记录错误和警告
```python
from utils.logger import warning, error, log_box

warning('[verifier] 发现潜在问题', print_to_console=True)
error('[refiner] 改进失败，配额耗尽', print_to_console=True)

log_box("Critical Error: System halted", width=60, print_to_console=True, level='ERROR')
```

## 迁移说明

从旧的`print`语句迁移到新的日志系统：

### 基本迁移
```python
# 旧代码
print('[solver] solver quota exhausted ...')
print(f'using: {elapsed}s, length: {length}')

# 新代码（基础版）
from utils.logger import log_print
log_print('[solver] solver quota exhausted ...', print_to_console=True)
log_print(f'using: {elapsed}s, length: {length}', print_to_console=True)

# 新代码（改进版，使用指标记录）
from utils.logger import warning, log_metric
warning('[solver] solver quota exhausted ...', print_to_console=True)
log_metric("执行时间", elapsed, "s", print_to_console=True)
log_metric("长度", length, print_to_console=True)
```

### 条件输出
```python
# 旧代码
if self.print_to_console:
    print('[solver] message')

# 新代码
log_print('[solver] message', print_to_console=self.print_to_console)
```

### 添加阶段分隔
```python
from utils.logger import log_section, log_separator

# 在主要阶段开始时
log_section("开始求解阶段", print_to_console=True)
# ... 执行逻辑 ...

# 在不同模块之间
log_separator('line', print_to_console=True)
```

## 技术实现

### 核心模块：`utils/logger.py`

- **get_logger()**: 获取/创建logger实例
- **log_print()**: 类似print的日志记录函数
- **reset_logger()**: 重置logger（用于创建新日志文件）
- 使用线程本地存储（ThreadLocal）确保每个线程独立的logger实例

### 日志文件命名
```python
timestamp = datetime.now().strftime("%Y%m%d_%H%M%S_%f")[:-3]
log_filename = f"logs/{timestamp}.log"
```

毫秒级精度确保高并发场景下的文件名唯一性。

## 日志示例

一个完整的日志文件示例：

```
====================================================================================================
                                      AlphaSolve 日志系统
====================================================================================================
启动时间: 2025-12-31 13:30:01.220
日志文件: 20251231_133001_220.log
====================================================================================================

2025-12-31 13:30:01.221 │ INFO     │ 📝 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2025-12-31 13:30:01.221 │ INFO     │ 📝 │                          Solver Phase                    │
2025-12-31 13:30:01.221 │ INFO     │ 📝 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2025-12-31 13:30:01.222 │ INFO     │ 📝 [AlphaSolve] create solver node, using model gpt-4
2025-12-31 13:30:01.223 │ INFO     │ 📝 [solver] 开始生成conjecture...
2025-12-31 13:30:03.456 │ METRIC   │ 📊 执行时间: 2.5s
2025-12-31 13:30:03.457 │ METRIC   │ 📊 答案长度: 1523
2025-12-31 13:30:03.458 │ SUCCESS  │ ✅ [solver] conjecture生成成功！
2025-12-31 13:30:03.459 │ INFO     │ 📝 ─────────────────────────────────────────────────────────────
2025-12-31 13:30:03.460 │ INFO     │ 📝 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2025-12-31 13:30:03.460 │ INFO     │ 📝 │                         Verifier Phase                   │
2025-12-31 13:30:03.460 │ INFO     │ 📝 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2025-12-31 13:30:03.461 │ INFO     │ 📝 [verifier] 开始验证conjecture...
2025-12-31 13:30:05.123 │ WARNING  │ ⚠️ [verifier] 发现潜在问题
2025-12-31 13:30:05.124 │ ERROR    │ ❌ [verifier] 验证失败
2025-12-31 13:30:05.125 │ INFO     │ 📝 ─────────────────────────────────────────────────────────────
```

## 最佳实践

1. **使用合适的日志级别**
   - `DEBUG`: 详细的调试信息（默认不输出到控制台）
   - `INFO`: 一般流程信息
   - `SUCCESS`: 成功完成的操作
   - `METRIC`: 性能指标和统计数据
   - `WARNING`: 警告但不影响继续执行
   - `ERROR`: 错误但可恢复
   - `CRITICAL`: 严重错误，可能导致程序终止

2. **使用结构化功能**
   - 主要阶段使用 `log_section()` 标识
   - 不同模块间使用 `log_separator()` 分隔
   - 重要消息使用 `log_box()` 突出显示
   - 指标数据使用 `log_metric()` 格式化输出

3. **保持日志清晰**
   - 使用模块标识（如[solver]、[verifier]）
   - 避免过多的DEBUG级别日志
   - 记录关键步骤和决策点
   - 包含足够的上下文信息

4. **性能考虑**
   - 大量输出时考虑设置 `print_to_console=False`
   - 避免在循环中频繁写入日志
   - 使用流式输出时注意 `end` 参数的使用
