[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "alphasolve"
version = "0.1.0"
description = "Multi-agent collaborative system for proving mathematical theorems and solving mathematical problems"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "wolframclient>=1.3",
    "rich>=13.0",
    "PyYAML>=6.0",
    "python-dotenv>=1.0",
    "openai-codex==0.147.0",
]

[project.scripts]
alphasolve = "alphasolve.cli:main"

[dependency-groups]
dev = ["pytest>=8"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
alphasolve = ["prompts/*.md", "config/*.yaml", "config/agents/*.yaml", "config/subagents/*.yaml", "solver/prompts/*.md", "solver/config/*.yaml", "solver/config/agents/*.yaml", "solver/config/subagents/*.yaml"]
