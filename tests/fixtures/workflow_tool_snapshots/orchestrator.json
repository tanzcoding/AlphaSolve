[
  {
    "description": "Audit a Markdown research/proof workspace before deciding current progress or next propositions.\n\nUse this as a compact map of verified results, knowledge notes, underexposed proof conclusions, verified infrastructure, and possible blocker/assembly language. Treat the result as navigation evidence, not as a deterministic verdict: read the cited files before relying on a claim, and compare it with research_reviewer reports when both are available. If the result includes a suggested next proposition with a selected target, inspect that target before broadening; if you choose a different research direction, state what evidence makes the selected target irrelevant, already statement-visible, or lower priority. If the selected target is choice-classification, do not rank conditional assembly or stronger machinery first unless the choice scan is already complete or irrelevant by cited evidence. If the selected target is an underclaimed answer-facing bound/value/threshold/stopping condition, do not rank it below speculative harder routes. A Statement gap that changes the original problem's visible bound, value, stopping condition, or theorem interface is not ordinary bookkeeping merely because harder downstream work remains.",
    "name": "ResearchProgressReview",
    "parameters": {
      "properties": {
        "max_files": {
          "default": 120,
          "description": "Maximum Markdown files to scan.",
          "maximum": 500,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "Workspace root or directory to audit. Use `.` for the current research workspace.",
          "type": "string"
        },
        "paths": {
          "default": [],
          "description": "Optional specific files/directories to scan instead of auto-detecting research folders.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Inspect Markdown files or narrow directories by surfacing headings, statement/progress sections, and proof tails.\n\nUse this after ResearchProgressReview, Grep, or a reviewer report cites specific files whose statements, proof endings, or progress notes matter for the next research decision.",
    "name": "InspectMarkdown",
    "parameters": {
      "properties": {
        "max_files": {
          "default": 8,
          "description": "Maximum Markdown files to inspect when path is a directory.",
          "maximum": 100,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "Markdown file or directory to inspect.",
          "type": "string"
        },
        "statement_lines": {
          "default": 80,
          "description": "Maximum lines to show from each selected statement/progress section.",
          "maximum": 300,
          "minimum": 1,
          "type": "integer"
        },
        "tail_lines": {
          "default": 40,
          "description": "How many ending lines to show for each file.",
          "maximum": 200,
          "minimum": 1,
          "type": "integer"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Searches readable text files under a specific file or directory.\n\nUsage:\n- Prefer Grep for exact symbol/string searches before reading many files.\n- Narrow `path` when you know the likely directory; broad searches can hit the result limit quickly.\n- Returns plain text in `path:line: text` format, optionally with context.\n- Use `context_lines` when the surrounding lines matter; keep it small for compact output.\n- `regex` defaults to true; set `regex=false` to force plain substring matching.\n- Results respect this agent's workspace access restrictions.",
    "name": "Grep",
    "parameters": {
      "properties": {
        "context_lines": {
          "default": 0,
          "description": "Number of context lines around each match.",
          "type": "integer"
        },
        "max_results": {
          "default": 50,
          "description": "Maximum results to return.",
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "File or directory to search in. Defaults to this agent's configured search root.",
          "type": "string"
        },
        "pattern": {
          "description": "The text or regular expression pattern to search for in file contents.",
          "type": "string"
        },
        "regex": {
          "default": true,
          "description": "Treat pattern as a regex (default: true; set false for substring matching).",
          "type": "boolean"
        }
      },
      "required": [
        "pattern"
      ],
      "type": "object"
    }
  },
  {
    "description": "Fast file pattern matching tool that works with any codebase size. Supports glob patterns like ``**/*.md`` or ``src/**/*.py``, or ``*`` to list directory contents.",
    "name": "Glob",
    "parameters": {
      "properties": {
        "max_results": {
          "default": 100,
          "description": "Maximum results to return.",
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "The directory to search in. Defaults to this agent's configured search root.",
          "type": "string"
        },
        "pattern": {
          "description": "The glob pattern to match files against.",
          "type": "string"
        }
      },
      "required": [
        "pattern"
      ],
      "type": "object"
    }
  },
  {
    "description": "Read text content from a file.\n\nTips:\n- In AlphaSolve research workspaces, this is an exact file-inspection tool, not the primary way to survey progress across many propositions or notes.\n- For broad progress review over `problem.md`, `verified_propositions/`, and `knowledge/`, run ResearchProgressReview first; then use Read on the cited files that need line-level precision.\n- A `<system>` tag will be given before the read file content.\n- The system will notify you when there is anything wrong when reading the file.\n- This tool is worth using in parallel only after a progress map or explicit citation has narrowed the files to inspect.\n- For Markdown proposition files with Statement and Proof sections, the default call with only `path` returns the Statement section plus the file tail; set `read_all=true` or pass `line_offset`/`n_lines` for exact proof inspection.\n- If you want to search for a certain content or pattern, prefer Grep over Read.\n- Content will be returned with a line number before each line like `cat -n` format.\n- By default, Read returns 250 lines.\n- `line_offset` is the first line to return.\n- `n_lines` is how many lines to return in this call; default is 250.\n- Set `read_all=true` to ignore `n_lines` and read from `line_offset` to the end of the file.\n- Without `read_all`, the maximum `n_lines` value is 1000.\n- Results respect this agent's workspace access restrictions.\n- For multi-file research progress review, prefer `ResearchProgressReview` before broad manual reading. Use `Read` after the progress map has identified specific files or when a single cited file needs exact line-level inspection.\n- If a Read result reports a proposition-index gap or an underclaimed proof tail, resolve whether that already-established conclusion should be surfaced before continuing broad exploration.",
    "name": "Read",
    "parameters": {
      "properties": {
        "line_offset": {
          "default": 1,
          "description": "The line number to start reading from. By default read from the beginning of the file. Set this when the file is too large to read at once.",
          "minimum": 1,
          "type": "integer"
        },
        "n_lines": {
          "default": 250,
          "description": "How many lines to return in this Read call. Defaults to 250. At most 1000 unless `read_all` is true.",
          "maximum": 1000,
          "minimum": 1,
          "type": "integer"
        },
        "path": {
          "description": "The path to the file to read (must be absolute or workspace-relative).",
          "type": "string"
        },
        "read_all": {
          "default": false,
          "description": "If true, ignore n_lines and return every line from line_offset through the end of the file.",
          "type": "boolean"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Writes a file within this agent's allowed write area.\n\nUsage:\n- Use `mode=\"overwrite\"` to replace the whole file.\n- Use `mode=\"append\"` to append content to the end of the file.\n- If this is an existing file, you MUST use the Read tool first to read the file's contents.\n- Treat index writes as objective bookkeeping after the research target is clear. Use SyncResearchState rather than Write for strategic state.md.",
    "name": "Write",
    "parameters": {
      "properties": {
        "content": {
          "description": "The content to write to the file.",
          "type": "string"
        },
        "mode": {
          "default": "overwrite",
          "description": "Whether to overwrite the whole file or append to the end.",
          "enum": [
            "overwrite"
          ],
          "type": "string"
        },
        "path": {
          "description": "The path to the file to write.",
          "pattern": "^verified_propositions(?:/[A-Za-z0-9][A-Za-z0-9._-]*)*/index\\.md$",
          "type": "string"
        }
      },
      "required": [
        "path",
        "content"
      ],
      "type": "object"
    }
  },
  {
    "description": "Performs exact string replacements in files.\n\nUsage:\n- The edit will FAIL if old_string is not unique in the file.\n- Provide a larger string with more surrounding context to make it unique.\n- Treat index edits as objective bookkeeping after the research target is clear. Use SyncResearchState rather than Edit for strategic state.md.",
    "name": "Edit",
    "parameters": {
      "properties": {
        "new_str": {
          "description": "The text to replace it with (must be different from old_string).",
          "type": "string"
        },
        "old_str": {
          "description": "The text to replace.",
          "type": "string"
        },
        "path": {
          "description": "The path to the file to modify.",
          "pattern": "^verified_propositions(?:/[A-Za-z0-9][A-Za-z0-9._-]*)*/index\\.md$",
          "type": "string"
        }
      },
      "required": [
        "path",
        "old_str",
        "new_str"
      ],
      "type": "object"
    }
  },
  {
    "description": "Lists files and directories under a workspace directory.\n\nUsage:\n- Returns plain text with one entry per line; directories end with `/`.\n- If the directory contains `index.md`, read `index.md` first before exploring other files.\n- The result respects this agent's workspace access restrictions.\n- If a research workspace contains both `verified_propositions/` and `knowledge/`, use `ResearchProgressReview` as the progress map before manually reading many index or proposition files.",
    "name": "ListDir",
    "parameters": {
      "properties": {
        "max_results": {
          "default": 200,
          "description": "Maximum entries to return.",
          "type": "integer"
        },
        "path": {
          "default": ".",
          "description": "Directory to list. Defaults to this agent's configured read root.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Creates a directory within the writable workspace area, including parent directories when needed.",
    "name": "MakeDir",
    "parameters": {
      "properties": {
        "path": {
          "description": "Directory path to create.",
          "pattern": "^verified_propositions(?:/[A-Za-z0-9][A-Za-z0-9._-]*)+/?$",
          "type": "string"
        }
      },
      "required": [
        "path"
      ],
      "type": "object"
    }
  },
  {
    "description": "Renames a file or directory in place within the writable workspace area.\n\nUsage:\n- Use this only when the item stays in the same directory and only its name changes.\n- `directory` is the parent directory that currently contains the item.\n- `old_name` and `new_name` must be plain names, not paths, and must not contain `/` or `\\`.\n- To move a file into another directory, use Move instead.\n- You may rename verified proposition Markdown files inside `verified_propositions/` and any of its subdirectories.\n- If two verified propositions have the same filename, use Rename to give one proposition a more specific name and prevent conflicts.\n- When renaming Markdown files inside `verified_propositions/`, Rename automatically updates matching `\\ref{old-path}` references across verified propositions to the new backslash-separated path.\n- This tool fails if the target path already exists.\n\nExamples:\n- Rename a folder: directory=\"verified_propositions\", old_name=\"graph-route\", new_name=\"graph-coloring-route\".\n- Rename a verified proposition file: directory=\"verified_propositions/graph-route\", old_name=\"cycle-bound.md\", new_name=\"odd-cycle-coloring-obstruction-bound.md\".",
    "name": "Rename",
    "parameters": {
      "properties": {
        "directory": {
          "description": "Parent directory containing the item to rename.",
          "pattern": "^verified_propositions(?:/[A-Za-z0-9][A-Za-z0-9._-]*)*/?$",
          "type": "string"
        },
        "new_name": {
          "description": "New file or directory name only; no path separators.",
          "pattern": "^(?!index\\.md$)(?!state\\.md$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "type": "string"
        },
        "old_name": {
          "description": "Current file or directory name only; no path separators.",
          "pattern": "^(?!index\\.md$)(?!state\\.md$)[A-Za-z0-9][A-Za-z0-9._-]*$",
          "type": "string"
        }
      },
      "required": [
        "directory",
        "old_name",
        "new_name"
      ],
      "type": "object"
    }
  },
  {
    "description": "Moves a file into another directory within the writable workspace area while keeping the same file name.\n\nUsage:\n- Use this when reorganizing files into topic folders.\n- `path` must be an existing file.\n- `destination_dir` must be an existing directory.\n- Move never renames the file; it always keeps the source file name.\n- When moving files inside verified_propositions/, Move automatically updates matching `\\ref{old-path}` references across verified_propositions/ to the new backslash-separated path.",
    "name": "Move",
    "parameters": {
      "properties": {
        "destination_dir": {
          "description": "Existing destination directory.",
          "pattern": "^verified_propositions(?:/[A-Za-z0-9][A-Za-z0-9._-]*)*/?$",
          "type": "string"
        },
        "path": {
          "description": "Existing file path to move.",
          "pattern": "^verified_propositions/(?!index\\.md$)(?!state\\.md$)(?!.*\\/index\\.md$)(?!.*\\/state\\.md$)(?:[A-Za-z0-9][A-Za-z0-9._-]*/)*[A-Za-z0-9][A-Za-z0-9._-]*\\.md$",
          "type": "string"
        }
      },
      "required": [
        "path",
        "destination_dir"
      ],
      "type": "object"
    }
  },
  {
    "description": "Start one worker and return immediately; this tool does not wait for the worker to finish.\n\nWorker lifecycle:\n- The worker first runs generator to draft one candidate proposition.\n- It then runs verifier; if verification fails and rounds remain, it runs reviser and repeats verifier -> reviser.\n- After verification, theorem-checking decides whether the verified proposition resolves the original problem.\n\nReturn content is JSON containing whether a worker was spawned, plus active_count, active_worker_ids, active_workers, max_workers, and available_worker_slots. If the parallelism limit has been reached, call TaskOutput before spawning more workers.",
    "name": "SpawnWorker",
    "parameters": {
      "properties": {
        "blocker_override_reason": {
          "description": "Required only when this direction has an active process-audit blocker and this spawn targets a different gap. State the concrete new evidence that justifies pivoting instead of directly attacking that blocker.",
          "type": "string"
        },
        "consolidation": {
          "description": "Optional (default false). Set true to spawn a CONSOLIDATION / DUAL attempt on a FIXED target: the worker may NOT weaken the statement, narrow it, add fresh hypotheses, or isolate a smaller sub-claim to pass verification. Only two results count as success: proving the pinned target exactly as stated, or refuting it with an explicit checkable witness (construction/counterexample). Use this to (a) cash out a direction whose verified props have accumulated, by attacking its terminal goal head-on, or (b) run the dual/falsification of a persistently-failing target. Pass the target text via pinned_target and give the direction's mainline props via frontier_refs.",
          "type": "boolean"
        },
        "direction_id": {
          "description": "Stable id of the research direction in state.md that owns this worker.",
          "maxLength": 80,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "type": "string"
        },
        "frontier_note": {
          "description": "Optional. One short note telling the worker what the curated frontier means for this hint or what to avoid (e.g. 'avoid the anchor-graph mainline; try construction/number-theoretic structure such as 45^2'). Shown alongside frontier_refs.",
          "maxLength": 2000,
          "type": "string"
        },
        "frontier_refs": {
          "description": "Optional. A curated list of verified-proposition references (paths relative to `verified_propositions` without the `.md` extension, subdirectories with backslashes, e.g. `structural\\anchor-graph\\anchor-graph-for-rectangle-partition`) that define the research frontier this worker should build on. The runtime injects each referenced proposition's Statement into the worker's task so the worker can focus on its assigned mathematical obligation instead of broad workspace exploration. Differentiate this per direction: give an exploit spawn its mainline frontier, give a new/orthogonal direction a small orthogonal set, and leave it empty for a free-exploration direction that should start from scratch.",
          "items": {
            "maxLength": 200,
            "type": "string"
          },
          "maxItems": 20,
          "type": "array"
        },
        "gap_id": {
          "description": "Stable id of the open gap inside that direction which this worker attacks.",
          "maxLength": 80,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "type": "string"
        },
        "global_attack": {
          "description": "Optional (default false). Set true to launch a GLOBAL consolidation that attacks `problem.md` directly — not any specific direction's terminal goal. This implies consolidation=true (no weakening allowed). If pinned_target is omitted, the runtime reads problem.md into pinned_target automatically. Use this when global_consolidation_directive.ready is true and you want to test whether accumulated verified propositions can already close the original problem. Do NOT pass direction_id or gap_id with global_attack.",
          "type": "boolean"
        },
        "hint": {
          "description": "Optional targeted hint for this worker only. Suggest a direction, method, branch, local target, or auxiliary assumption. This is different from the user's hint.md.",
          "maxLength": 4000,
          "type": "string"
        },
        "method_id": {
          "description": "Optional proof/search method arm. If omitted, the hierarchical scheduler recommendation is used.",
          "enum": [
            "direct_proof",
            "contradiction",
            "construction",
            "computation",
            "falsification",
            "consolidation"
          ],
          "type": "string"
        },
        "parent_id": {
          "description": "Backward-compatible single parent attempt state_id. Direction arm ids are not attempt ids.",
          "maxLength": 64,
          "type": "string"
        },
        "parent_ids": {
          "description": "Optional parent attempt state_ids for the attempt DAG. Pass multiple ids for crossover. These describe lineage only; direction_id independently identifies the research direction.",
          "items": {
            "maxLength": 64,
            "type": "string"
          },
          "maxItems": 8,
          "type": "array"
        },
        "pinned_target": {
          "description": "Optional. The exact target statement to attack when consolidation=true. If omitted, the worker uses the goal expressed in `hint`. Ignored when consolidation is false.",
          "type": "string"
        },
        "route_id": {
          "description": "Stable id returned by RegisterResearchRoute. All targeted workers exploring the same reviewer-proposed path should carry the same route_id so their branches can be compared.",
          "maxLength": 80,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$",
          "type": "string"
        },
        "rubric": {
          "description": "Optional. A concise acceptance checklist (3-6 bullet points, each starting with '- ') specifying what the proven Statement must satisfy to count as progress on the assigned gap. The rubric is NOT shown to the worker — it is stored and returned to you in TaskOutput for your own structured self-assessment via RecordResearchImpact. Example: '- Explicit construction of a permutation and tiling\n- Rectangle count k <= 2111\n- All non-hole cells covered exactly once'. Write criteria that are checkable against the proven Statement alone.",
          "type": "string"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Launch a new specialized agent and return its final report.\n\nThe launched agent runs in a separate session. It does not see this conversation unless you include the needed context in `prompt`.\n\nAvailable agent types:\n- compute_subagent: Concrete symbolic or numeric computation and verification.\n- curator: Incrementally update the knowledge wiki from local traces and synthesize portfolio checkpoints by comparing worker outcomes, process audits, and orchestrator strategy transitions.\n- numerical_experiment_subagent: Bounded exploration, branch checks, local numerical experiments, small-n exhaustive search, counterexample mining, invariant discovery, and constraint-gap quantification. Use when analytic progress is blocked and a numerical probe could reveal which unproven lemma is most likely to close the gap. Can run exhaustive enumeration, LP formulations, and structural invariant extraction on small-n analogues.\n\n- reasoning_subagent: Bounded proof and mathematical reasoning obligations.\n- research_reviewer: Survey verified_propositions/ and knowledge/ when there are many files, compare against problem.md, identify the current global blocker, adversarially review your own draft plan (via the Agent tool), and use numerical_experiment_subagent for any decision-relevant bounded numerical or combinatorial uncertainty before finalizing one research plan.\n\n## Writing the prompt\n\n- For broad workspace review, split by directory or file group: ask each subagent to inspect one scoped area and return a concise evidence report with file paths, line references when available, current status, open issues, and recommended next local step.\n- Use the main agent to compare subagent reports and decide the global next step; do not ask a subagent with a narrow scope to make the final workspace-wide decision.\n- State the exact claim, definitions, assumptions, and what needs to be proved or checked.\n- Include relevant context: what's already known, what approaches have been tried, which files to reference, and why this task matters.\n- Give enough context that the agent can make judgment calls rather than just following a narrow instruction.\n- Keep the task self-contained and limited to the agent's scope.\n- `research_reviewer` returns a single research plan, not a list to pick from. It has its own scoped `Agent` tool (restricted to `reasoning_subagent`/`numerical_experiment_subagent`) and must return both an \"Adversarial Review\" and a `Route Validation` section. `VERDICT: CLEAR` only means the plan survived critique; before dispatch, obey `Route Validation`'s `DISPATCH:` status. For `BLOCKED` or `NEEDS_FALSIFICATION`, record the supplied evidence with `RecordDispatchConstraint`; only `READY_FOR_PROOF` permits a normal proof worker. This remains a prompt-level report, so check that its cited numerical coverage and counterexample search are substantive before trusting it.\n- When using `research_reviewer`, delegate an independent progress audit rather than a proof attempt for the route you currently favor.\n- Include any already-seen proposition-index gaps or underclaimed proof-tail evidence in the prompt, especially any `ResearchProgressReview` suggested next proposition and selected target, and ask the reviewer to rank that candidate against broader theorem work.\n- If underclaimed proof-tail evidence only quantifies why one conditional route is obstructed, and the problem still asks for an existential choice among models, parameters, data, weights, or ansatzes, ask the reviewer to use that obstruction as a constraint on the choice-classification target rather than replacing the constructive scan.\n- If underclaimed proof-tail evidence changes a numerical bound, objective value, threshold, stopping condition, or answer-facing inequality, do not demote explicit Statement consolidation below speculative research directions unless another verified Statement already records it.\n- When the workspace has many verified local estimates for one shared energy, bootstrap, reduction, or interface system, ask whether the next proposition should assemble the exact system levels, constants, remainders, and restrictions before attacking another local lemma.\n- If a local repair appears to be one component of a larger system interface, ask the reviewer whether the durable next proposition should be the exact interface with that repair listed as a premise, rather than the repair in isolation.\n- If downstream propositions look ready to promote but rely on a support, domain, time-scale, interface, or parameter-choice prerequisite that is still conditional, slackened, or not Statement-visible, ask the reviewer to rank that prerequisite before the downstream promotion unless the prerequisite is already established in verified_propositions.\n- If the problem asks for existence of a model, parameter regime, input family, weight, or ansatz, and the workspace already has a conditional theorem chain, ask the reviewer whether the next proposition should classify choices that make the existing chain's hypotheses hold before inventing stronger general estimates.\n- A choice-classification target should cover the whole theorem interface, including support/domain conditions, lower-bound or positivity hypotheses, nonlinear remainder control, regularity/smallness assumptions, and the timescale on which the resulting inequality must hold.\n- Do not let a framework-formalization, conditional assembly, or gap-naming task outrank a choice-classification task when it would only restate that some model, parameter, data, weight, or ansatz choice must satisfy the existing chain's hypotheses.\n- Do not replace that scan with re-proving verified infrastructure, sharper constants, alternative test functions, or stronger PDE/ODE machinery unless the choice-classification scan is already complete or rules out viable choices.\n- When a verified proposition Statement conflicts with an index or knowledge summary, treat the verified Statement as authoritative and ask whether the summary is stale rather than planning from the weaker summary.\n- If a reviewed `unverified_propositions/` file already has a passed review and records a system interface, constants, derivative levels, assumptions, or obstruction, ask whether promoting or repairing that proposition is the best verified-progress step before opening a new local route.\n- `research_reviewer` now returns one primary plan (plus ≤2 one-line rejected alternatives), not a ranked list — treat that plan as a prior, not a command. Each `selection_advice` from `TaskOutput` carries a read-only `frontier` list ranking active worker nodes by cheap proxy signals (open subgoals, verified implications, falsification hits) — nothing is pre-selected for you. If the reviewer's plan targets a direction whose recent workers only produced necessary_condition_only/orthogonal outputs, while `frontier` shows another direction with stronger verified implications and no falsification hits, prefer the latter. The reviewer's plan reflects a static workspace snapshot; `frontier` reflects what workers actually achieved. Update your belief accordingly.\n- If the reviewer's plan is an exact system or interface assembly, do not demote it as bookkeeping merely because it may expose a harder local gap; that assembly may be the research proposition needed to determine which local gap is actually blocking the proof.\n- Do not present an unverified route as the core blocker unless a verified proposition, knowledge synthesis, or prior reviewer report explicitly identifies it as such.\n- Whether `research_reviewer` reads `verified_propositions/**/state.md` (your prior strategic notes) is decided automatically by the system via an epsilon-greedy policy and recorded in the run log. You do not need to set `read_state` for `research_reviewer` — the system assigns it and overrides any value you pass.",
    "name": "Agent",
    "parameters": {
      "properties": {
        "description": {
          "description": "A short (3-5 word) description of the task.",
          "type": "string"
        },
        "prompt": {
          "description": "The task for the agent to perform.",
          "type": "string"
        },
        "read_state": {
          "description": "Optional; only meaningful for research_reviewer. If true, the reviewer may consult verified_propositions/**/state.md (prior strategic notes). If false or omitted, the reviewer forms an independent assessment and does not read state.md, avoiding being steered by a previous wrong route.",
          "type": "boolean"
        },
        "type": {
          "description": "The type of specialized agent to use for this task.",
          "enum": [
            "compute_subagent",
            "curator",
            "numerical_experiment_subagent",
            "reasoning_subagent",
            "research_reviewer"
          ],
          "type": "string"
        }
      },
      "required": [
        "type",
        "description",
        "prompt"
      ],
      "type": "object"
    }
  },
  {
    "description": "Start one worker for open-ended, non-targeted exploration and return immediately (does not wait). Use it only for a materially orthogonal idea, not to bypass a blocked targeted route. The runtime refuses this tool while a completed process audit still needs curator blocker classification or while a stalled audit names a repeated avoided obligation; in the latter case, launch the prescribed targeted consolidation instead.\n\nThe worker receives persisted failed-method taboos, active repeated blockers, and an optional under-explored verified seed. It must choose a distinct bounded claim or record why no such claim is available. Returns the same shape as SpawnWorker; if no slot is available it returns spawned=false.",
    "name": "SpawnFreeExploration",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Persist exactly one route proposed by research_reviewer before assigning targeted workers to it. A route is the coarse mathematical path; use SpawnWorker(route_id=...) repeatedly to maximize different attempts under it. Copy the machine-readable route contract from the reviewer report.",
    "name": "RegisterResearchRoute",
    "parameters": {
      "properties": {
        "based_on_state_id": {
          "type": "string"
        },
        "direction_id": {
          "type": "string"
        },
        "evidence_refs": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gap_id": {
          "type": "string"
        },
        "route_claim": {
          "type": "string"
        },
        "route_id": {
          "type": "string"
        },
        "stop_condition": {
          "type": "string"
        },
        "success_condition": {
          "type": "string"
        },
        "target": {
          "type": "string"
        }
      },
      "required": [
        "route_id",
        "based_on_state_id",
        "direction_id",
        "gap_id",
        "route_claim",
        "target",
        "success_condition",
        "stop_condition"
      ],
      "type": "object"
    }
  },
  {
    "description": "Compare the accumulated worker branches of one reviewer route against other routes and the terminal gap. Record whether the path is advancing, partial, stalled, refuted, redundant, or still inconclusive. This is a route-level judgment; continue to use RecordResearchImpact for individual verified workers.",
    "name": "AssessResearchRoute",
    "parameters": {
      "properties": {
        "close_route": {
          "type": "boolean"
        },
        "evidence_paths": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "route_id": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        },
        "verdict": {
          "enum": [
            "ADVANCING",
            "PARTIAL",
            "STALLED",
            "REFUTED",
            "REDUNDANT",
            "INCONCLUSIVE"
          ],
          "type": "string"
        }
      },
      "required": [
        "route_id",
        "verdict",
        "summary"
      ],
      "type": "object"
    }
  },
  {
    "description": "Assess one completed verified worker result against the direction-level gap it was assigned. Use this after TaskOutput for every worker listed in pending_research_impacts, before spawning more targeted work. Correctness is already decided by the verifier; classify whether the correct result solves, directly advances, only gives a necessary condition, weakens, duplicates, refutes, or misses the target. gap_effect='closed' is only accepted when relation_to_target is solves_target, sufficient_for_target, or direct_advance; any other relation reporting gap_effect='closed' is rejected.",
    "name": "RecordResearchImpact",
    "parameters": {
      "properties": {
        "closed_gap_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "consolidation_outcome": {
          "description": "Optional. Set only when assessing a consolidation/dual attempt (a worker spawned with consolidation=true). 'solved' = the pinned target was proved as stated; 'blocked_gap' = not proved but a concrete attackable gap was localized (record it in new_gaps); 'wall' = the attempt repeatedly hit the same insurmountable point / could not connect the props to the target — this flags the direction for a mandatory dual/falsification probe and lowers its budget.",
          "enum": [
            "solved",
            "blocked_gap",
            "wall"
          ],
          "type": "string"
        },
        "continuation_value": {
          "enum": [
            "high",
            "medium",
            "low",
            "none"
          ],
          "type": "string"
        },
        "discharges_standing_hypothesis": {
          "description": "Optional (default false). For an impossibility-kind direction, set true only if this result unconditionally discharges the direction's standing_hypothesis (e.g. eliminates a case without assuming k<=T) rather than deriving one more property under it. If false, a 'direct_advance'/'sufficient_for_target' claim is auto-downgraded to necessary_condition_only, because deriving another conditional property is not progress toward the contradiction.",
          "type": "boolean"
        },
        "failed_method_family": {
          "description": "Required when method_failure_type is set. The method family that failed (e.g., 'anchor-graph', 'induction', 'rsk', 'topological', 'lp-dual', 'information-theoretic', 'algebraic'). Recorded in the direction's failed_methods tabu list.",
          "type": "string"
        },
        "gap_effect": {
          "enum": [
            "closed",
            "advanced",
            "unchanged",
            "invalidated"
          ],
          "type": "string"
        },
        "method_failure_type": {
          "description": "Optional. Tabu search: classify a failure (when relation_to_target is necessary_condition_only/weaker_than_target/orthogonal/duplicate/unrelated/unknown and continuation_value is low/none). 'method_blocked' = the method was insufficient but the goal may still be correct → the runtime records the method_family in the direction's failed_methods (tabu list) so future spawns must use a different method. 'conclusion_refuted' = the goal itself may be false → also records to tabu list and flags the direction for review. Requires failed_method_family to be set.",
          "enum": [
            "method_blocked",
            "conclusion_refuted"
          ],
          "type": "string"
        },
        "new_gaps": {
          "items": {
            "properties": {
              "gap_id": {
                "type": "string"
              },
              "statement": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "relation_to_target": {
          "enum": [
            "solves_target",
            "sufficient_for_target",
            "direct_advance",
            "necessary_condition_only",
            "weaker_than_target",
            "refutes_target",
            "orthogonal",
            "duplicate",
            "unrelated",
            "unknown"
          ],
          "type": "string"
        },
        "remaining_gaps": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "requires_direction_review": {
          "type": "boolean"
        },
        "rubric_assessment": {
          "description": "Optional. Structured self-assessment against the rubric that was attached to this worker's SpawnWorker call. For each criterion in the rubric, state whether the proven Statement passes it and cite evidence. The runtime tracks consecutive zero-pass workers per direction: 2+ in a row automatically flags the direction for re-review.",
          "properties": {
            "checks": {
              "items": {
                "properties": {
                  "criterion": {
                    "type": "string"
                  },
                  "evidence": {
                    "type": "string"
                  },
                  "passed": {
                    "type": "boolean"
                  }
                },
                "type": "object"
              },
              "type": "array"
            },
            "passed_count": {
              "type": "integer"
            },
            "total_checks": {
              "type": "integer"
            }
          },
          "type": "object"
        },
        "summary": {
          "type": "string"
        },
        "worker_id": {
          "description": "Completed worker id from TaskOutput.",
          "type": "string"
        }
      },
      "required": [
        "worker_id",
        "relation_to_target",
        "gap_effect",
        "continuation_value",
        "summary"
      ],
      "type": "object"
    }
  },
  {
    "description": "Persist a reviewer or numerical-probe finding that controls future targeted dispatch for one exact direction/gap. Use status='refuted' only with a verified proof, explicit witness, or exhaustive finite counterexample; it invalidates that exact gap and blocks repeat dispatch. Use status='needs_falsification' for sampled/incomplete numerical evidence; ordinary proof workers are then blocked until a method_id='falsification' worker checks the claim. This is not a mathematical proof tool: it records the evidence scope so the scheduler cannot mistake a sample for a universal theorem.",
    "name": "RecordDispatchConstraint",
    "parameters": {
      "properties": {
        "claim": {
          "description": "The exact claim whose dispatch policy is being constrained.",
          "type": "string"
        },
        "direction_id": {
          "type": "string"
        },
        "evidence": {
          "description": "Compact counterexample or experiment scope, coverage, and conclusion.",
          "type": "string"
        },
        "evidence_level": {
          "description": "'refuted' requires verified, explicit_witness, or exhaustive_finite evidence; sampled evidence may only require a falsification probe.",
          "enum": [
            "verified",
            "explicit_witness",
            "exhaustive_finite",
            "sampled"
          ],
          "type": "string"
        },
        "gap_id": {
          "type": "string"
        },
        "source_paths": {
          "description": "Workspace-relative files/logs that contain the evidence.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "status": {
          "enum": [
            "refuted",
            "needs_falsification"
          ],
          "type": "string"
        }
      },
      "required": [
        "direction_id",
        "gap_id",
        "claim",
        "status",
        "evidence_level",
        "evidence"
      ],
      "type": "object"
    }
  },
  {
    "description": "Replace the qualitative research plan with a structured portfolio of multiple directions. Each direction owns its gaps, progress summary, evidence, next actions, review trigger, and stop condition. The runtime preserves measured impact counters and atomically regenerates verified_propositions/state.md. Use index.md separately for verified proposition facts; never put strategic plans in index.md.",
    "name": "SyncResearchState",
    "parameters": {
      "properties": {
        "directions": {
          "items": {
            "properties": {
              "direction_id": {
                "type": "string"
              },
              "direction_kind": {
                "description": "Optional. Set 'impossibility' for a direction that tries to prove a value optimal or a configuration impossible (e.g. 'k<=T is impossible'); then also give standing_hypothesis and terminal_gap_id so advance is judged honestly and the consolidation/dual machinery can engage.",
                "enum": [
                  "unknown",
                  "impossibility",
                  "lower_bound",
                  "upper_bound",
                  "construction",
                  "existence",
                  "classification"
                ],
                "type": "string"
              },
              "evidence_refs": {
                "description": "Verified proposition paths relevant to this direction (relative, no extension).",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "failed_methods": {
                "description": "Tabu list: method families that have failed on this direction's goal. Each entry: {method_family, failure_type, summary, gap_id, recorded_at}. The runtime preserves this across SyncResearchState calls; you usually do NOT need to set it manually — use RecordResearchImpact with method_failure_type instead. Only set explicitly when creating a new direction that inherits the tabu list of a retired one.",
                "items": {
                  "type": "object"
                },
                "type": "array"
              },
              "goal": {
                "type": "string"
              },
              "health": {
                "type": "string"
              },
              "knowledge_refs": {
                "description": "Knowledge note paths relevant to this direction (relative, no extension).",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "progress_summary": {
                "type": "string"
              },
              "standing_hypothesis": {
                "description": "Optional. The assumption every result in an impossibility direction carries (e.g. 'k <= 3035'). A result that only derives another property under it, without discharging it or closing the terminal gap, will not count as advance.",
                "type": "string"
              },
              "status": {
                "type": "string"
              },
              "steps": {
                "description": "Ordered research steps. Each step is both a plan item and a gap to close. Refine via SyncResearchState as workers return results.",
                "items": {
                  "properties": {
                    "method": {
                      "type": "string"
                    },
                    "statement": {
                      "type": "string"
                    },
                    "status": {
                      "enum": [
                        "open",
                        "advanced",
                        "closed",
                        "invalidated"
                      ],
                      "type": "string"
                    },
                    "step_id": {
                      "type": "string"
                    }
                  },
                  "type": "object"
                },
                "type": "array"
              },
              "stop_condition": {
                "type": "string"
              },
              "terminal_gap_id": {
                "description": "Optional. The gap_id of the direction's terminal obligation (the contradiction / final goal). Progress on peripheral gaps while this stays open is not advance.",
                "type": "string"
              },
              "title": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "type": "array"
        },
        "dispatch_plan": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "objective_status": {
          "type": "string"
        },
        "objective_summary": {
          "type": "string"
        }
      },
      "required": [
        "objective_summary",
        "directions"
      ],
      "type": "object"
    }
  },
  {
    "description": "Wait until one active worker finishes, or until the timeout is reached.\n\nUse this tool to collect worker lifecycle results. If the maximum number of active workers has been reached, call TaskOutput before spawning more workers.\n\nReturn content is JSON. It always includes completed, active_count, active_worker_ids, active_workers, max_workers, and available_worker_slots. Verified but unsolved completions also appear in selection_advice.pending_research_impacts and must be assessed with RecordResearchImpact before more targeted spawning. Consolidation workers that failed (rejected, or verified but did not achieve their pinned target) are auto-assessed by the runtime with consolidation_outcome=wall and appear with auto_assessed=true; they do NOT require RecordResearchImpact. Consolidation workers that are verified but have target_achieved=null DO require your RecordResearchImpact assessment (you must judge whether the proven statement matches the pinned target). It may include timed_out when no worker finishes before the timeout; solved and solution_path when the original problem is solved; human_expert_updates when hint.md or knowledge/references changed during the run; verified_propositions_organization when verified proposition directories should be organized before more spawning; and progress_audit with the independent process auditor's latest verdict, terminal gap, and persisted audit files. Completed workers with an unresolved mathematical handoff appear in difficulty_portfolio. When at least two candidate handoffs are available, the runtime requests an independent research_reviewer comparison and returns its report; use it to decide whether to attack a shared obligation, keep routes distinct, or pivot. A handoff is not a canonical blocker until curator curation confirms it. When process_audit_context_reset is present, the runtime will clear this orchestrator conversation before the next model request, automatically run one fresh independent research_reviewer assessment, and inject its report into the new context. Read the cited audit and reviewer report, verify their evidence, and SyncResearchState before targeted spawning.",
    "name": "TaskOutput",
    "parameters": {
      "properties": {
        "seconds": {
          "default": 3600.0,
          "description": "Maximum seconds to wait before returning active worker status.",
          "maximum": 7200,
          "minimum": 1200,
          "type": "number"
        }
      },
      "required": [],
      "type": "object"
    }
  },
  {
    "description": "Returns the current date and time in ISO 8601 format.",
    "name": "GetCurrentTime",
    "parameters": {
      "properties": {},
      "required": [],
      "type": "object"
    }
  }
]