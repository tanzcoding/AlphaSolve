version: 1
agent:
  name: compute_subagent
  system_prompt_path: ../../prompts/compute_subagent.md
  model_config: COMPUTE_SUBAGENT_CONFIG
  max_turns: 40
  when_to_use: Concrete symbolic or numeric computation and verification.
  tools:
    - RunPython
    - RunWolfram
    - Read
    - Glob
    - Grep
