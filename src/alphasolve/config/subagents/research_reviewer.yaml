version: 1
agent:
  name: research_reviewer
  system_prompt_path: ../../prompts/research_reviewer.md
  model_config: ORCHESTRATOR_CONFIG
  max_turns: 40
  when_to_use: Survey verified_propositions/ and knowledge/ when there are many files, compare against problem.md, and recommend research directions.
  tools:
    - Read
    - Glob
    - Grep
    - Bash
