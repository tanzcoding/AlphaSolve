settings:
  max_verify_rounds: 2
  verifier_scaling_factor: 4
  verifier_agents:
    - verifier_failure_modes
    - verifier_stepwise
  subagent_max_depth: 2

agents_dir: ./agents
subagents_dir: ./subagents
